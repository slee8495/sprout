# Sprout — 백업 / 장애 대응 계획 (2026-08-05 기준)

프로덕션 데이터는 두 곳에 있음: Neon(Postgres, 모든 텍스트/메타데이터) + Vercel Blob(사진/음성 파일). 각각 별도 정리.

## 1. Neon (데이터베이스)

- 현재 **Free 플랜**. 프로젝트: `neon-fuchsia-yacht` (Vercel Storage 마켓플레이스로 연결됨).
- **PITR(시점 복구) 창: 6시간.** Neon 콘솔 → Backup & Restore에서 지난 6시간 내 임의 시점으로 즉시 복구 가능.
- 6시간이 지난 데이터는 Neon의 자동 히스토리로 복구 불가능. 예약 스냅샷(자동 백업)은 유료 플랜(Launch 이상)에서만 지원 — 지금은 스냅샷 0개, 스케줄 없음.
- **수동 스냅샷은 Free 플랜에서도 가능** — Neon 콘솔 → Backup & Restore → "Create snapshot"으로 언제든 즉시 1회성 스냅샷 생성 가능, 저장해두면 나중에 그 시점으로 복구 가능.

### 권장 조치

1. **위험한 작업(스키마 마이그레이션, 대량 삭제 로직 배포/테스트) 직전에 수동 스냅샷 생성** — 이번 세션에서 해온 "로우 카운트 확인" 관행을 대체/보강하는 더 강력한 방법. 무료.
2. **6시간 창이 불안하면 Neon Launch 플랜으로 업그레이드 고려** — PITR 창이 길어지고(플랜별로 다름, 콘솔에서 정확한 일수 확인 필요) 예약 스냅샷도 켤 수 있음. **비용이 발생하는 결정이라 실행 전 사용자 승인 필요** — 아직 안 함.
3. 실제 장애 시 복구 절차: Neon 콘솔 → 해당 프로젝트 → Backup & Restore → Source branch: `main` → Point in time에 복구할 시각 선택 → Preview data로 먼저 확인 → Restore. (Restore는 새 브랜치를 만드는 게 아니라 현재 브랜치 자체를 되돌리는 동작이므로 실행 전 반드시 Preview로 확인할 것.)

## 2. Vercel Blob (사진 / 음성 메모)

- 내부적으로 **AWS S3 기반**, **99.999999999%(11 nines) 내구성**, 99.99% 가용성. 인프라 자체가 데이터를 잃어버릴 확률은 사실상 0에 가까움 — 이 부분은 별도 조치 불필요.
- **진짜 리스크는 인프라가 아니라 애플리케이션 버그(잘못된 `del()` 호출)임.** Vercel Blob은 버전 관리나 휴지통(soft-delete) 기능을 제공하지 않음 — 한 번 지우면 SDK 레벨에서 되돌릴 방법이 없음.
- 코드베이스에서 `del()`을 호출하는 곳은 현재 **셀프서비스 계정 삭제 기능 하나뿐**(`src/app/settings/deleteAccountActions.ts`) — 가족 이름을 정확히 타이핑해야 버튼이 활성화되는 확인 절차가 있어 실수로 트리거되기 어려움.

### 권장 조치

1. 앞으로 `del()`을 호출하는 코드를 새로 추가할 때는 반드시 사용자 확인 절차(타이핑 확인 등)를 동반할 것 — 되돌릴 방법이 없다는 전제로 설계.
2. 별도의 자동 백업 파이프라인(예: 매일 밤 전체 DB를 JSON으로 덤프해서 Blob에 저장)은 **일부러 만들지 않음** — 민감한 가족 데이터(이메일, 아이 생일, 일기 본문)를 저장하는 장소를 하나 더 늘리는 것 자체가 새로운 유출 리스크이고, Neon의 수동 스냅샷 기능으로 이미 같은 목적을 더 안전하게 달성할 수 있음.

## 요약

| | 인프라 내구성 | 실수 복구 가능 창 | 무료로 늘리는 방법 |
|---|---|---|---|
| Neon (DB) | 높음 (Neon 자체 복제) | 6시간 (Free plan PITR) | 위험한 작업 전 수동 스냅샷 |
| Vercel Blob (파일) | 매우 높음 (S3, 11 nines) | 없음 (삭제 즉시 영구) | `del()` 호출 경로에 확인 절차 유지 |

두 시스템 모두 "인프라가 데이터를 잃어버리는" 시나리오보다 "사람/코드가 실수로 지우는" 시나리오가 훨씬 현실적인 리스크. 그래서 이 문서의 결론은 새 백업 시스템을 만드는 것보다 **위험한 작업 전 수동 스냅샷 습관화 + 삭제 코드 경로에 확인 절차 유지**로 정리함.
